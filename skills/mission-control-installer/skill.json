{
  "name": "mission-control-installer",
  "version": "1.0.0",
  "description": "Install and configure Opzava — the OpenClaw agent orchestration dashboard",
  "author": "Builderz Labs",
  "license": "MIT",
  "tools": ["exec", "fs"],
  "parameters": {
    "deployment_mode": {
      "type": "string",
      "enum": ["docker", "local"],
      "default": "docker",
      "description": "How to deploy Opzava"
    },
    "port": {
      "type": "number",
      "default": 3000,
      "description": "Port for the Opzava dashboard"
    },
    "install_dir": {
      "type": "string",
      "default": "",
      "description": "Installation directory (defaults to ./mission-control)"
    }
  },
  "tags": ["mission-control", "dashboard", "installer", "docker"]
}
