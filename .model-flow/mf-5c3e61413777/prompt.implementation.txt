[DELEGATED_RUNNER_IDENTITY]

Implement GH #199's diagnosable half in the current worktree only.

Scope is exactly these four files unless an existing test suite makes a different test file strictly necessary:
- apps/gateway-broker/src/routing/connection-manager.ts
- apps/gateway-broker/src/acl/openclaw/operator-client.ts
- apps/gateway-broker/src/__tests__/connection-manager.routing.fake-gateway.test.ts
- apps/gateway-broker/src/__tests__/operator-client.fake-gateway.test.ts

At both existing tenant-mismatch checks, immediately before the existing return err(...), call the already-present BrokerLogger warn once with exact metadata keys and values:
{ routeId: route id, routeTenantId: route.tenantId, principalTenantId: input.actingPrincipal.tenantId }
Exact message: "Gateway route tenant does not match the acting principal."

Add one short WHY comment at each site in local voice: browser denial is deliberately opaque, so without both ids a stale OPENCLAW_GATEWAY_TENANT_ID versus seeded org is undiagnosable from logs.

Hard constraints:
- Do not alter either guard's order or condition.
- Do not alter returned error code, exact client-facing message, or details.
- Never add tenant ids to gatewayBrokerError details.
- No new constructor parameter/dependency, database access, env var, boot validation, or runtime/env.ts edits.
- Use this.logger in connection-manager and the existing client logger in operator-client.
- Keep readonly/local style.

Tests at the existing public seams, test-first in vertical slices:
1. Route acquisition mismatch warns once with exact metadata and exact new log message, returns the unchanged gatewayBroker.tenantMismatch error (exact existing message, no tenant ids in details).
2. Operator-client internal boundary mismatch does the same.
3. Matching tenants warn zero times.
Use an existing/injected recording BrokerLogger and the real fake Gateway patterns; no new production injection surface.

Run focused tests through Turbo, not bare Vitest, and do not commit. Host owns final verification and commit.
