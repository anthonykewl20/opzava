# Q18 docs contradiction audit (2026-07-04)

> Status: all 5 findings APPLIED same day (see EXECUTION.md worklog 2026-07-04, mainframe-move entry).

Cross-check of CLAUDE.md, CONTEXT.md, ARCHITECTURE.md, ADR-016, the Status lines of ADR-002/ADR-015, port-openclaw-control-ui-program.md, EXECUTION.md (Current State + first 3 worklog entries), and real-world-validation/SKILL.md — against repo reality (docker-compose.yml services, mainframe/ with UPSTREAM.md+PATCHES.md+Dockerfile, real-world-validate.local.mjs).

docs/plan/EXECUTION.md(Reference Map: "docs/adr/ | Accepted architecture decisions ADR-001 through ADR-015") vs CLAUDE.md("docs/adr/ — 16 ADRs") + ARCHITECTURE.md(ADR index lists ADR-001..ADR-016) + docs/adr/ADR-016-mainframe-tracked-fork.md(exists, Accepted Q18) -> bump the Reference Map to "ADR-001 through ADR-016" so it matches the 16-ADR reality its own first worklog entry records.
docs/plan/EXECUTION.md(Current State Status "not-started (move slice)" + Next concrete action enumerates the mainframe-move steps as TODO) vs repo reality(git 0c41ce2a "squash-import OpenClaw v2026.6.11 as tracked fork (ADR-016)"; mainframe/ present with UPSTREAM.md bd2740fedc/v2026.6.11 + PATCHES.md + Dockerfile; docker-compose.yml openclaw-platform-gateway is `build: ./mainframe`; pnpm-workspace.yaml excludes mainframe/) -> the move slice has landed, so set Status done and advance Active slice/Next action to Slice 3.7 view #1, calling out only the real-world-validation gate proof if still owed.
ARCHITECTURE.md(deployment topology: "Local runs the stack with a `local` Traefik profile, mkcert wildcard TLS on *.localhost") vs docker-compose.yml(no `profiles:` key on any service; traefik starts unconditionally and the web router is HTTP-only on :18088 with no TLS cert resolver or mount) -> either add the local profile + mkcert TLS into the canonical compose (which ADR-015 declares the single deployment contract) or state in ARCHITECTURE.md that they live in a local override / are planned-not-yet-composed like pgbouncer/redis.
docs/plan/EXECUTION.md(Current State Port-program field: OpenClaw Control UI "~13 views: overview/agents/channels/config-models/sessions/workboard/cron/mcp/nodes/skills/usage/logs") vs docs/plan/consensus/port-openclaw-control-ui-program.md(row #13 "Debug / exec-approval / dreaming" is a ported OpenClaw view) + CONTEXT.md("port program rows 1–14") -> add debug/exec-approval/dreaming (and Ask Admin WebChat #14) to the EXECUTION enumeration so agents don't silently drop row #13 from port scope.
CLAUDE.md(doc map: "mainframe/ — ... (ADR-016; arrives via the mainframe-move slice)") vs repo reality(mainframe/ already exists and the move slice is committed) -> change the future tense to past/present (e.g. "landed via the mainframe-move slice") so agents don't read mainframe/ as not-yet-present.

Total findings: 5
