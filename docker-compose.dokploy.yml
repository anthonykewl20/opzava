# Local Dokploy-parity stack.
#
# Compatibility wrapper for the canonical Compose topology. The parity harness
# still runs `docker compose -f docker-compose.dokploy.yml ...`; this file keeps
# that public command stable while loading the base stack plus parity override.
include:
  - path:
      - docker-compose.yml
      - docker-compose.parity.yml
